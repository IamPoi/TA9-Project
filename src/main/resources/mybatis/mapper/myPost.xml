<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<!-- <select id="myPostView" parameterType="map" resultType="list"> -->
<mapper namespace="com.example.demo.mapper.MyPostMapper">
	<select id="myPostView" resultType="com.example.demo.model.PostDTO">
		select * from post where writer = #{userId} order by post_num desc limit 5
	</select>
	
</mapper>