<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.demo.mapper.MyPageMapper">
	<!-- <select id="postView" parameterType="map" resultType="list"> -->
	<select id="aptInfo" parameterType="String" resultType="com.example.demo.model.AptInfoDTO">
		select * from apt_info where dong_apt = #{dong_apt}
	</select>
	
</mapper>